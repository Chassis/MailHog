description "Mailhog"

start on runlevel [2345]
stop on runlevel [!2345]
respawn

pre-start script

bash << "EOF"
  mkdir -p /var/log/mailhog
  chown -R vagrant /var/log/mailhog
EOF

end script

exec su - vagrant -c 'mailhog &>>/var/log/mailhog/mailhog.log'